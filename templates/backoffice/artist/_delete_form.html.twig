<form method="post" action="{{ path('app_backoffice_artist_delete', {'id': artist.id}) }}" onsubmit="return confirm('Confirmez-vous la suppression ? Ceci est irréversible');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ artist.id) }}">
    <button class="btn btn-outline-danger">Supprimer</button>
</form>
