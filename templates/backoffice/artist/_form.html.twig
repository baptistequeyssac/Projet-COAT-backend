{{ form_start(form) }}
    <div class="list-group">
        {{ form_widget(form) }}
    </div>
    <button class="btn btn-outline-success">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}
